Working plan of 2026-2-2
Tasks
1.Please improve the design by adding a new feature based on the previous deisgn that please create a new medical device application webpage based on application.pdf User can modify, download or upload application datasets (csv, json). Then system will create application form in markdown with 20 entities in a table with title and context. User can modify the application form in markdown or text view. Then user can paste or upload review guidance (text, markdown, pdf). User can modify, download the review guidance (text, markdown). Then agent will use the modified review guidance to execute on the previous application form and create review summary in markdown (user can modify prompt and select models). Then user can keep prompt on the reveiew report (user can select models). Please design this system based on previous app.py. Please don't modify or create codes and only create a comprehensive technical specification for the improved design in 2000~3000 words. This agentic ai system to be deployed on hugging face space using streamlit, SKILL.md, agents.yaml, OPENAI API, GEMINI API. User can provide api key on webpage if not get from the environment. Don't show api key if get from the environment. First user paste (text, markdown) or upload files(text, markdown, pdf), user can preview the upload pdf in pdf view. Then system will use python package to trim the pdf and do OCR (user decide which pages to trim and do OCR, user can choose to use python packages or LLM based OCR). Then system will reorganized the doc into markdown with keywords in coral color. User can modify the doc in markdown or text view. Then user can keep prompt on the doc or select agents from agents.yaml to execute on the doc (user can modify the prompt and select models gemini-2.5-flash, gemini-3-flash-preview, gpt-4o-mini). Please also create a feature that user can modify (modify the prompt and select models), download, upload agents.yaml, SKILL.md. If the upload agents.yaml is not standarized agents.yaml, please transform it into standardized agents.yaml then user can modify the agents.yaml.
2.Please create technical specification in markdown in 2000~3000 words for a agentic ai system that will first find all pdf files in the working folder. Then system will trim frist page of each pdf and create summary of each trimmed files in markdown (save in .md). Then system will create a table of content doc (ToC doc) in markdown combined all previous created summary. Then user can keep prompt on the ToC doc (user can select models) or select agents from agent.yaml to execute on the ToC doc. agentic ai system to be deployed on hugging face space using streamlit, SKILL.md, agents.yaml, OPENAI API, GEMINI API.User can provide api key on webpage if not get from the environment. Don't show api key if get from the environment. 